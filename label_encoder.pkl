from sklearn import metrics
from sklearn.naive_bayes import GaussianNB
from sklearn.naive_bayes import MultinomialNB
from sklearn.preprocessing import OneHotEncoder
from sklearn.preprocessing import LabelEncoder
import pandas as pd
import numpy as np

url='https://raw.githubusercontent.com/sak1b0/Thesis/master/allCSV/automobile.csv'

#dataframe hisebe read hocche
#nrow represents koto gula instance niye kaaz kotesi
df=pd.read_csv(url,header=None,nrows=3)

#testing block start

#encode korar aage data dekhlam
print(df.values)

#kun kun column k encode korbo sheita ber kore store korar jonno array
my_arr=[]

#shob attribute er datatype check korlam
print(df.dtypes)

#colum er index er track rakhte 
index=0
for item in df.dtypes:
  if(item=='object'):
    #data type jodi nominal mane object hoy tahole array te add kortesi
    my_arr.append(index)
  index=index+1

#kun kun column nominal sheita dekhlam
print(my_arr)
df=df.values
print('\n============ after encoding =============\n')

#hot=OneHotEncoder(column_transformer=[0])
#df=hot.fit_transform(df).toarray()
lbl=LabelEncoder()
#loop chalaye required column gula te encode korlam
for item in my_arr:
  df[:,item] = lbl.fit_transform(df[:,item])

print(df)


#testing block end
